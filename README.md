# RINet
Deterministic Python toolkit for constructing residue interaction networks (RINs) from protein structures and generating interpretable, structure-first residue coupling profiles using explicit graph operators, with reproducible execution and exportable analysis artifacts.
